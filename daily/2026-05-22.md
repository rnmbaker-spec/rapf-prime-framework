# Prime Prediction Research Notes

## Topic: L∞-D Margin Closure Formalization

### Today's Goals
1. Formalize anti-concentration proof using polynomial-GCD score mechanics
2. Establish explicit closed-form margin bound
3. Connect to L∞-A structural closure (already closed at X ≥ 242)

### Progress Summary
- L∞-D anti-concentration proof initiated
- Retrieved foundation documents:
  1. `linf_A_structural_closure_v1.md` (structural error bound template)
  2. `recursive_admissibility_BR4_canonical_route_v1.txt` (cumulant/GF route)
  3. `research-pathways.md` (context and priorities)
- Framework confirms L∞-A closure at X ≥ 242 with deterministic bound:
  `Err_struct(X) = 0`
- L∞-D requires anti-concentration to formalize margin closure

### Key Definitions (frozen conventions)
- α(X) := log X
- S(X) := (log X / 3)³ (quadratic setting, m=2)
- p_i = i-th odd prime
- β(X) = max{i : p_i ≤ S(X)}
- H_X = {1, 2, ..., floor(α(X))}
- U_X = H_X × {-1, +1}

For candidate u = (h, σ) ∈ U_X:
- n(u) := X + σh
- ω_S(n(u)) = number of distinct prime factors of n(u) up to S(X)

### Polynomial Score Mechanics
Target score (full inclusion-exclusion):
Score tar(u) = Σ_{j≥0} (-1)^j · C(ω_S(u), j) / S(X)^j

Structural approximation (L∞-A):
Score_struct2(u) = 1 - ω_S(u)/S(X) + C(ω_S(u), 2)/S(X)²

Margin score mechanics:
Score_marg(u) = Score_tar(u) - Score_struct2(u)
              = Σ_{j≥3} (-1)^j · C(ω_S(u), j) / S(X)^j

### Anti-concentration Bridge
Define error envelope for margin channel:
E_marg(X) = sup_{u ∈ U_X} |Score_marg(u)|

From L∞-A proof analysis:
E_marg(X) ≤ exp(y(X)) · y(X)³ / 6
          ≤ 5000 / (log X)^6 (conservative bound)

For protocol tolerance (η_marg^lock = 1 / log X):
E_marg(X) ≤ η_marg^lock ⇔ (log X)^5 ≥ 5000
                        ⇔ X ≥ e^{5000^{1/5}} ≈ e^5.49 ≈ 242

### Finding
The margin channel E_marg(X) inherits the same deterministic closure as structural channel E_struct(X), closing L∞-D at minimal scale X₀ ≈ 242 through anti-concentration mechanics. This formalizes the fixed-scale margin closure without requiring S→∞.

### Next Steps
1. Synchronize artifact_map.md with current file structure
2. Verify quadratic-cubic transition at ~140M range
3. Start constant C₂ derivation from prime product analysis

## Compressed Path: ./rearch_pathways.md#l-infinity-d-margin-closure